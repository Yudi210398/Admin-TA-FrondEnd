import React, { Fragment } from "react";

const TombolDelete = ({ idproduct }) => {
  return (
    <Fragment>
      <button className="btn btn-danger">Delete Produk</button>
    </Fragment>
  );
};

export default TombolDelete;
